[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "db: integration test requiring a PostgreSQL test database (set TEST_DATABASE_URL).",
    "pdf: PDF generation test requiring a Playwright Chromium browser (auto-skipped if absent).",
]
